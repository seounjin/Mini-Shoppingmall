import styled from 'styled-components';

const Wrapper = styled.div`
  position: relative;
  overflow: hidden;
  ul {
    position: relative;
    display: flex;
    list-style: none;
    padding: 0;
    margin: 0;
  }
`;

export default Wrapper;
